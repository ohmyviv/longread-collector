# V0.6 Project Handoff — 2026-08-28 S1 Day-0 Closure Checkpoint

Status: **superseding engineering checkpoint for S1 Day-0**  
Production posture: **SHADOW / NOT_READY / unchanged**  
Semantic/runtime baseline: `a380c68920c1de26f1e703b721d7eb2195900002`  
Current repository main before this docs sync: `7b892663a734005e71fd5c687a1e76000b95f9db`  
Collector semantic runtime: `collector-v0.6-pr7.3.9`

This checkpoint supersedes the 2026-08-27 S1 evidence-readiness checkpoint where it said the first run had arrived but formal deterministic audit was pending. Historical checkpoints remain immutable evidence of their own time.

## 1. S1 Day-0 decision

The first prospective natural S1 run has now completed formal Day-0 acceptance:

`COL-20260827-224813-BJT-zh_midday`

**Decision: DAY-0 PASS / ELIGIBLE EXPOSURE.**

The meaning is narrow and frozen: the paired experiment executed correctly and left reconstructable evidence. This is not S1 completion, route-utility approval or S2 admission.

Eligible exposure state after this run:

- Yicai: 1 / technical observed;
- Jiemian-depth: 1 / technical observed;
- Caixin: 1 / technical observed;
- EEO: 0 / not yet prospectively observed.

Future runs are independently audited. Source maturity advances by eligible exposure, not raw run count.

## 2. Day-0 L0–L3 evidence

### L0 Scheduler / cohort: PASS

- exact durable run count=1;
- group=`zh_midday`;
- post-activation start;
- natural scheduled run, not manual rerun;
- naturally selected S1 targets=`yicai`, `jiemian-depth`, `caixin`.

The scheduled 11:50 BJT run actually started at 22:48:13 BJT, start delay=39,490 seconds. Scheduler delay remains reliability telemetry and does not invalidate an otherwise valid paired natural experiment.

### L1 Control validity: PASS

- final status success;
- source-run coverage version `run-source-coverage-v0.2`;
- coverage persisted=TRUE;
- declared coverage rows=8, durable rows=8;
- snapshot persistence success;
- expected=177, persisted=177;
- readback=TRUE.

Matching v0.6 Shadow summary independently records full snapshot invariant=TRUE, capture gap=0, Control acquired/shared body=24/24, body fingerprint mismatch=0, Shadow request count=0, Shadow Firecrawl request count=0, incremental cost=0 and Control result preserved=TRUE.

### L2 Experimental isolation: PASS

- route rows only for naturally selected experimental sources;
- Treatment rows `metadata_only`;
- Control body attempts=24 <= fixed cap 32;
- native source cap=4 unchanged;
- absolute host cap=4 unchanged;
- static 21-surface S1 contract passes.

Per-run Sheet evidence still does not independently prove the three architectural claims `Treatment body_requests=0`, no candidate-selection entry and no production article_cache entry. They remain explicit code/workflow regression contracts rather than fabricated runtime observations. The separate Shadow summary nevertheless records zero Shadow network/Firecrawl requests and preserved Control result.

### L3 Telemetry integrity: PASS

Expected active surfaces for selected targets=15: Yicai 6 + Jiemian 4 + Caixin 5.

Persisted:

- observation rows=15;
- item rows=275;
- one observation per expected surface;
- all run FKs correct;
- Route Contract / Discovery / body mode / Telemetry mismatches=0.

An independent read-only recomputation from all 275 item rows exactly reproduced all 15 surface aggregates: raw/unique, recent, dated, exact-high, Control overlap, Treatment unique, noise count and noise-reason composition. No item-parent or aggregate mismatch exists.

Formal audit record:

`docs/PHASE3_ZH_ROUTE_S1_DAY0_AUDIT_20260827.md`

## 3. L4/L5 are not Day-0 gates

Persisted L4 status distribution before timestamp forensic correction:

- observed=5;
- date_unknown=7;
- stale_surface=3;
- empty=0;
- request_failed=0.

All 15 Treatment metadata requests returned HTTP 200 and parsed successfully.

The raw item-ledger L5 recomputation before timestamp-confidence correction was:

- item rows=275;
- unique canonical observed=248;
- canonical Control overlap=8;
- persisted proven-recent canonical=40;
- persisted proven-recent incremental canonical=40;
- explicit-noise canonical among those recent incrementals=1.

These 40/40 freshness-derived numbers are **not utility-grade evidence** because Day-0 exposed a material timestamp-association measurement defect. They must not be used for S2, route promotion, product eligibility or fixed-32 displacement decisions.

## 4. Timestamp forensic finding

The first exposure proves the generic section-time association cannot be trusted uniformly across publishers.

Read-only deterministic recomputation over the 275 persisted rows found:

### Caixin

- rows with explicit first-party URL path date=94;
- persisted date conflicts with URL path date=72;
- URL path date exists but persisted publication time is unbound=22.

This explains the false `stale_surface` classification on companies/finance/china. For example, first-party article URLs encode `/2026-08-27/...` while the generic context parser persisted `2026-08-02` or `2026-08-01`.

### Yicai

- item titles with explicit `N分钟前/N小时前` local evidence=65;
- relative-age evidence available but publication time unbound=64;
- the sole bound relative-age row is visibly wrong: title says `3小时前` at observed 2026-08-27 22:48:21 BJT, while persisted time is 2026-08-26 22:43:00 BJT via `listing_relative_clock`.

Therefore current S1 listing timestamps are publisher-dependent measurement evidence, not a uniform reliable freshness truth.

## 5. PR #139 — read-only timestamp forensic guard

PR #139 `Phase 3: add read-only S1 timestamp forensic guard` was CI-green and squash merged:

`7b892663a734005e71fd5c687a1e76000b95f9db`

It adds:

- `src/longread_collector/zh_route_shadow_timestamp_forensic_v1.py`;
- `tests/test_zh_route_shadow_timestamp_forensic_v1.py`.

The module is pure offline/read-only. It detects:

- URL path date vs persisted date conflict;
- URL path date available but unbound;
- article-local relative-age vs persisted binding conflict;
- article-local relative-age available but unbound.

It is deliberately **not wired into Treatment discovery** and does not rewrite old telemetry. Thus PR #139 changes neither the S1 experimental treatment nor Control semantics and creates no new measurement cohort. It only prevents contaminated timestamp observations from being silently interpreted as route health or utility.

No live parser repair is merged at this checkpoint. Any future change to timestamp binding semantics must be a separately versioned/reviewed measurement-contract change; until then, future L4/L5 interpretation should apply the forensic guard.

## 6. Why Day-0 still passes

The timestamp problem belongs to L4/L5 measurement interpretation. It does not alter the facts that:

- the correct natural Control run exists;
- Control is durable and internally complete;
- Treatment remained isolated;
- expected Treatment surfaces were all attempted;
- sidecar telemetry is complete and reconstructable.

Accordingly the correct state is:

**Day-0 PASS + timestamp-derived utility evidence contaminated**, not Day-0 FAIL.

## 7. Current next work

No user action is required now.

Continue natural prospective accumulation. For every later eligible run:

1. audit the run independently under L0–L3;
2. assign eligible exposure only to naturally selected target sources in a valid run;
3. apply timestamp forensic guard before interpreting L4/L5 freshness metrics;
4. look for EEO's first eligible natural exposure;
5. only after repeated source/surface exposures discuss technical repeatability or preliminary utility.

Do **not** mechanically wait for a fixed number of runs and do not mechanically advance after a fixed number.

Issue backlog remains:

- #53 ProPublica body-usability/fallback invariant — higher-priority future Acquisition work;
- #40 Yahoo Finance hosted paid-release/market-report classification — future source-relationship work.

Both may have isolated offline/regression design, but merging Control-changing implementations during the current S1 evidence window remains a separate reviewed decision.

## 8. Frozen production boundary

Unchanged:

- Collector remains Shadow;
- Final Recall v1.3.1 remains offline/Shadow measurement only;
- scheduled Final Recall remains v1.2;
- 07:35 Editor does not consume article_cache;
- `V06_PRIMARY_ENABLED=false`;
- `AUTO_PROMOTE_WHEN_READY=false`;
- `EDITOR_0735_CONNECTED=false`;
- body-attempt cap remains 32;
- source cap / host cap / ranking / source registry / L4 / L5 / L6 remain unchanged.

## 9. Current decision

**S1 Day-0 = PASS.**  
**S1 = active evidence accumulation.**  
**Technical repeatability = not yet established.**  
**S2-ready = NOT ESTABLISHED / NOT AUTHORIZED.**  
**Production = SHADOW / NOT_READY.**
