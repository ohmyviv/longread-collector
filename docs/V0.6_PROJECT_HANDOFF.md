# Longread Collector v0.6 — Project Handoff

> Current engineering checkout for the longread Collector / daily recommendation project. The Google Doc `每日长文推荐 — Canonical Handoff` (document id `1bhedf-8OWiWAWCYxwgboRfc5Et4yxDcZCi3de4sOisE`) is the mutable canonical handoff after evidence reconciliation. This repository file is a durable current-state engineering summary. Historical benchmark documents remain immutable evidence.

**Last substantive update: 2026-08-15.**

## 0. Resume protocol and authority

At the start of a new engineering session:

1. read this file;
2. read the Canonical Handoff;
3. verify current GitHub `main`, open PRs and latest CI;
4. verify latest scheduled natural Collector evidence and live Sheet/config;
5. reconcile any disagreement before making a release decision.

Authority order for mutable facts:

`merged code/contracts → open PR → GitHub Actions → scheduled natural evidence → live Sheet/config → docs → handoff → chat`.

If this file disagrees with higher-authority evidence, correct this file; do not force the evidence to match the document.

Repository: `ohmyviv/longread-collector`.

## 1. Current code and release posture

Latest semantic code merge before this docs-only synchronization:

```text
4b581db494668a9da8897b88ced251e80dce6912
PR #94 — feat: add offline standard longread eligibility contract
```

Recent relevant merges:

```text
PR #89  cec50fb56df3676a0b33927c9adc9f998a1a3141  Phase0B bounded deadline freshness reserve
PR #91  9ce9c43282f15b63963c56cd9f850b21e6436574  Phase0B final run-ledger audit persistence
PR #92  7baa4bc320296cfcc0b0f6ac25b1ba99d0bef8fd  Final Recall v1.2 item observation windows
PR #93  f408ccd801caff8cd51f228a4228b6fe0cd69c58  artifact-only full-funnel observability
PR #94  4b581db494668a9da8897b88ced251e80dce6912  offline Standard Longread Eligibility E0
```

Current semantic identities:

```text
Collector runtime:          collector-v0.6-pr7.3.9
Canonical source:           canonical-source-v0.6-pr7.3.9
Canonical article resolver: canonical-article-resolver-v0.6-pr7.3.9
Publication:                canonical-publication-v0.6-pr7.3.7
Surface:                    canonical-surface-v0.6-pr7.3.4
Snapshot writer:            snapshot-persistence-v0.6-pr7.3.8
Phase0A audit:              snapshot-persistence-v0.6-phase0a
Frozen L5:                  editorial-judge-v0.6-pr7.2
Legacy control:             collector-v0.5.6m
Phase0B policy:             deadline-freshness-reserve-v0.6-phase0b.1
```

Production posture remains:

```text
mode: shadow
Transport Gate: READY
Editorial Gate: NOT_READY
Promotion Gate: SHADOW
article_cache production consumption: PROHIBITED
auto_promote_when_ready: FALSE
```

No docs-only merge changes these values.

## 2. Settled target architecture

```text
Source Registry
      ↓
GitHub Collector
  L1 Discovery
  L2 Acquisition Gate
  L3 Acquisition
  raw observations / replayable snapshots
      ↓
L4 Canonical Article
      ↓
Standard Longread Eligibility
      ↓
L5 Editorial Judge
      ↓
Candidate Universe
      ↓
L6 Policy / Portfolio
      ↓
history / archive / delivery
```

Collector outputs observations/evidence; L4 resolves facts. Do not flatten uncertain publication-date/source evidence into final canonical facts before L4.

The intended future primary Discovery authority is GitHub Collector. Scheduled ChatGPT/native Discovery remains an independent benchmark/transitional path until promotion evidence supports a staged switch.

## 3. Frozen boundaries

Until an explicit reviewed change says otherwise:

- do not start PR-8;
- do not reopen L4 for isolated P2 / low-frequency P1 edge cases;
- do not change frozen L5 as a substitute for Discovery/reliability evidence;
- do not change L6 without product evidence;
- do not raise native source cap above 8 to hide Recall problems;
- do not increase Firecrawl/body/network budgets to manufacture Recall;
- do not let the 07:35 Editor consume `article_cache` before promotion;
- do not enable automatic promotion;
- do not use old raw candidate counts or legacy `53/75 not_discovered` as valid Recall KPIs.

## 4. Phase0A — DONE / NATURAL ACCEPTED

PR #86 established the durable Discovery snapshot readback invariant without changing the PR-7.3.8 writer identity.

Natural acceptance:

```text
COL-20260813-184710-BJT-zh_evening
Actions 31692575791
expected=152
persisted=152
readback=true
full_snapshot_invariant=true
```

Promotion-grade strict snapshot integrity begins at `2026-08-13 18:47:10 BJT`. Earlier snapshots may be used diagnostically, not automatically as strict Recall evidence.

## 5. Phase0B — DONE / NATURAL ACCEPTED

PR #89 added a bounded deadline freshness reserve inside the existing source cap. No ninth source scan and no Acquisition/network/Firecrawl/body budget increase were introduced.

Corrected formal closure evidence:

```text
GitHub Actions: 31837235761
collector_run_id: COL-20260815-041810-BJT-pre_report
started:  2026-08-15 04:18:10 BJT
completed: 2026-08-15 04:29:02 BJT
```

The prior handoff tuple `COL-20260815-042614-BJT-pre_report / 156` was incorrect. Higher-authority GitHub artifact plus live `collector_runs` reconcile the correct tuple above.

Formal closure properties:

```text
freshness_reserve:
  wired
  newyorker
  restofworld
  quanta
  atlantic
  propublica

coverage_rotation:
  yale-e360
  404media

total sources = 8
snapshot expected/persisted = 208/208
readback = true
full_snapshot_invariant = true
capture gap = 0
shadow network requests = 0
shadow Firecrawl requests = 0
incremental shadow cost = 0
body fingerprint mismatches = 0
```

The exact Phase0B policy marker, source reasons and scan ages are durable in the live run ledger. Phase0B acceptance is closed.

A pre-existing fallback-ledger/schema-mapping mismatch remains separate technical debt. It does not reopen the freshness selector acceptance and must not be used to broaden the already-passing selector repair.

## 6. Collector Baseline Evaluation v1 — COMPLETE

Durable evaluation:

`每日长文推荐 — Collector Baseline Evaluation v1`

Google Doc id: `160hPZ0ni0pwYwlGia59MkMIBQJe4Dl2uotxLFwzY4G8`.

Primary baseline is the corrected natural `pre_report` run above.

Engineering / Transport result:

```text
PASS AS SHADOW BASELINE
Transport/engineering: PASS
Product/editorial promotion: NOT READY
Promotion effect: NONE
```

Observation-aware funnel using merged PR #93 semantics:

```text
12 attempted Discovery surfaces
→ 208 raw observations
→ 208 unique URL hints
→ 202 gate acquire + 5 hard_reject + 1 defer
→ 32 control-body observed
→ 32 Acquisition success
→ 32 Canonical success
→ 32 Editorial decision-eligible
→ 14 consider/actionable + 18 low_value
→ 10 selected
Projection emitted: 0
```

Selection actions across the 208 observations:

```text
select_standard=10
reject=23
defer=175
```

The large defer count is dominated by bodies not observed in the control acquisition subset. Missing acquisition observations are an observation boundary, not 175 Acquisition or editorial failures.

Reference `COL-20260814-042612-BJT-pre_report`:

```text
190 raw
186 gate acquire
32 control bodies
30 acquisition success + 2 partial
30 canonical/editorial decision-eligible
14 actionable
10 selected
```

2026-08-15 therefore shows a mechanically stable shadow pipeline: raw universe 190→208 while actionable remains 14 and selected remains 10, with engineering invariants clean. These ratios are descriptive only because the 32 body-observed items are capacity/control-selected, not a random sample of all 208 observations.

The 10 shadow-selected items all carry frozen L5 verdict `consider`; none is `recommend`. This does not establish Human Utility and must not be combined with the 80 delivered-LR human-review Hit Rate.

## 7. Collector Promotion Review v1 — NOT_READY / REMAIN SHADOW

Durable review:

`每日长文推荐 — Collector Promotion Review v1`

Google Doc id: `1C3H3aHSGLNI-7FYOsKtQsMw4htpx_wG8FuGuHiX_zus`.

Current gate reconciliation:

```text
A Engineering / Transport:                    PASS / READY
B Final Recall v1.2 prospective strict Recall: PENDING
C Human Utility / Incremental Human-Useful:    PENDING
D Multi-day Collector vs independent A/B:      PENDING
E Standard Longread eligibility readiness:     PARTIAL / E0 ONLY
F Version / health reconciliation:             FAIL / stale legacy state
G Manual approval:                             NOT REQUESTED

Overall: NOT_READY / remain SHADOW
```

Live `collector_health` still carries the legacy blocker `editorial_gate_not_ready_v056j_review_pending`, while the runtime is now `collector-v0.6-pr7.3.9`. `collector_evaluations` also lacks a formal current-pr7.3.9 Human/Editorial release evaluation. Existing `collector_version_reconciliation_policy` requires GitHub merged version, config, latest run classification and evaluations to reconcile before promotion; therefore manually changing a single READY cell is prohibited.

Legacy promotion thresholds such as 3 shadow days, 48h eligible count, technical-domain count, fixed-fixture 85% accuracy/precision, source-chase and wire-dedup targets remain historical diagnostics. They are not sufficient current-v0.6 promotion criteria because the project has since changed snapshot integrity, L4 provenance, freshness selection, Recall denominator, funnel semantics and Human Recommendation evidence.

Recommended staged adoption, not yet activated:

```text
Shadow
  ↓
Production Candidate Input
  - Collector candidates may enter the 07:35 candidate universe
  - native Discovery remains in parallel
  - candidate_origin/provenance retained
  - identical eligibility/L5/L6 rules
  ↓
Primary Discovery
  - only after production-context multi-day A/B proves adequate
    selected/strong/Chinese recall, Human Utility and stability
```

Every stage requires explicit manual approval; `auto_promote_when_ready` remains false.

## 8. Final Recall v1.2 — MEASUREMENT REPAIR MERGED / PROSPECTIVE BASELINE PENDING

PR #92 introduced `final-recall-audit-v1.2-item-window`:

- denominator uses actual `published_date → 07:35 cutoff` per final item;
- maximum observation window 14 calendar days;
- time_track is QA/editing metadata, not observation clock;
- strict matching uses immutable Discovery snapshots after publication;
- Phase0A strict snapshot epoch separates strict from diagnostic history;
- outside-registry and effective-route state are separate denominator axes.

Historical v1.1 `53/75 not_discovered` is invalid as a Recall KPI because it contains proven false misses.

A true v1.2 workflow dry-run/read-path execution and prospective strict baseline are still pending. Do not invent them.

## 9. Full-funnel observability — MERGED

PR #93 is merged and artifact-only. It adds observation-aware audit semantics without wiring the audit into production runtime or Sheets.

Important semantics:

- no acquisition event = body not observed by the control subset, not Acquisition failure;
- `strong/editorial actionable` is the audit alias for frozen L5 `recommend|consider`;
- `recommend` is separately high_editorial_value;
- Projection is currently not emitted by the shadow runner and must be reported as absent, not fabricated.

## 10. Human Recommendation evidence — 80/80 COMPLETE

Fixed retrospective set: 80 delivered recommendations from 2026-08-02 through 2026-08-12.

```text
强烈值得: 9
值得:     33
一般:     18
不应推荐: 20
Human Hit = 42/80 = 52.5%
```

Attribution of 38 non-Hits:

```text
upstream_eligibility       17
editorial_mismatch         12
preference_or_novelty       8
borderline_other             1
```

After removing upstream eligibility failures: `42/63 = 66.7%`.

Editorial-evaluable set after also removing preference/novelty: `42/55 = 76.4%` conditional Hit.

Strongest failure families:

- wrong_medium_or_asset=6; all 6 human Rejects;
- too_short=11, but simple character hard gate disproven;
- too_newsy=9, 7 human Rejects;
- too_shallow=3, all 3 human Rejects.

This evidence satisfies the prerequisite for targeted eligibility/L5 offline evaluation. It does not mean raw 52.5% equals L5 accuracy, and it does not justify changing production L5 while scheduled execution reliability remains the current P0.

## 11. Standard Longread Eligibility — E0 MERGED / OFFLINE ONLY

PR #94 defines:

```text
eligible_standard
route_special
ineligible_standard
unknown
```

E0 does not modify runtime/config/workflow/L4/L5/L6 or `article_cache` wiring. Length is measurement-only.

The 11 human `too_short` items ranged from 2,600 to 9,000 historical `body_chars_read`, while known Hits existed at 3,000–5,200. A `<9,500` threshold would catch all 11 but lose 22/42 known Hits. Therefore no simple fixed character hard gate is accepted.

Next evidence tracks remain separate:

- E1: high-confidence recurring briefing / academic asset / video-first factual recognition and routing;
- E2: substantive-length provenance + structural adequacy measurement first;
- L5.1: only later, narrow depth-floor/newsiness offline evaluation on the 55 editorial-evaluable human items.

## 12. Scheduled LR path — separate P0 from Collector

The 07:35 ChatGPT/native recommendation path is not the Collector production path.

LR-v3.5.1 first scheduled natural acceptance on 2026-08-15 failed:

```text
LR-20260815-0735-BJT-LRv35
Coverage Gate PASS
raw=8
verified=2
strong=2
selected=2
model_requested=unspecified
reasoning_effort_requested=unspecified
model_observed=unavailable
reasoning_effort_observed=unavailable
final persistence FAIL: FINAL_PERSISTENCE_SHEETID_MISMATCH
```

Independent manual benchmark:

```text
LR-20260815-0811-BJT-LRv35
requested GPT-5.6 Thinking + High
Coverage PASS
raw=26
verified=23
strong=13
selected=8
persistence/readback/archive PASS
```

This rejects the claim that only two worthwhile articles existed that day and confirms a scheduled execution-quality gap, without proving High reasoning is the sole cause.

LR-v3.5.2 is now implemented as a Sheet runtime contract + 07:35 automation launcher repair for:

- low-yield Discovery effort completion;
- delta-only candidate checkpoints;
- dynamic Sheet tab locator resolution;
- freeze-before-persistence/readback;
- truthful model audit.

No Collector production wiring was introduced. First LR-v3.5.2 natural acceptance target is `2026-08-16 07:35 BJT`. Freeze its config/automation until that evidence arrives.

## 13. Current next steps

### P0 — LR scheduled reliability

Do not modify LR-v3.5.2 before its first natural 07:35 acceptance unless a new P0 requires it. Manual High is benchmark evidence, not acceptance substitution.

### Collector Shadow evidence window

Over the next 3–7 natural days:

1. keep Collector in Shadow;
2. produce daily artifact-only observation-aware funnel summaries;
3. accumulate Final Recall v1.2 prospective strict item-window evidence from Phase0A-post snapshots;
4. compare Collector universe with independent native/manual-High reference where available;
5. attribute misses to registry/route/revisit/observation/L4/editorial/portfolio layers;
6. human-review only plausible Collector-exclusive high-value candidates rather than all raw URLs;
7. derive Incremental Human-Useful Recall and report Chinese recall separately.

### Promotion control plane

After prospective evidence exists, create a separate reviewed v0.6 Promotion Reconciliation change that:

- replaces/reclassifies stale v0.5.x health blocker language without deleting history;
- reconciles config/run/evaluation versions;
- creates a current v0.6-pr7.3.9 release evaluation;
- defines explicit Stage-2 rollback criteria.

Only after those gates pass should manual approval for `Production Candidate Input` be considered. Do not jump directly from Shadow to Primary Discovery.

### Eligibility / Phase0C

E1/E2 may continue as isolated offline design/replay. Do not production-wire them merely to improve promotion metrics. Phase0C/NBD is unblocked by Phase0B but remains an independent source/route decision; do not mix it into the current LR-v3.5.2 natural acceptance experiment.

## 14. Durable evidence references

Repository evidence:

- `docs/benchmarks/2026-08-13_collector_reconciliation.md`
- `docs/benchmarks/2026-08-13_phase0a_natural_acceptance.md`
- `docs/benchmarks/2026-08-13_phase0b_freshness_measurement.md`
- `docs/FULL_FUNNEL_AUDIT_V1.md`
- `docs/STANDARD_LONGREAD_ELIGIBILITY_E0.md`
- `docs/COLLECTOR_BASELINE_EVALUATION_V1.md`
- `docs/COLLECTOR_PROMOTION_REVIEW_V1.md`

Canonical durable design/evaluation docs:

- Canonical Handoff id `1bhedf-8OWiWAWCYxwgboRfc5Et4yxDcZCi3de4sOisE`
- Collector Baseline Evaluation v1 id `160hPZ0ni0pwYwlGia59MkMIBQJe4Dl2uotxLFwzY4G8`
- Collector Promotion Review v1 id `1C3H3aHSGLNI-7FYOsKtQsMw4htpx_wG8FuGuHiX_zus`
- Longread Eligibility Audit v1 id `1k_xlX_Kc7DCXKcDftiSzVfJCNw6RrsQfd4TC9_Lvc18`

## 15. Release decision at this checkpoint

```text
Phase0A: DONE / NATURAL ACCEPTED
Phase0B: DONE / NATURAL ACCEPTED
Collector Shadow baseline: ESTABLISHED / TRANSPORT HEALTHY
Collector Promotion: NOT_READY / REMAIN SHADOW
Final Recall v1.2: MERGED / PROSPECTIVE STRICT BASELINE PENDING
Human evidence: 80/80 COMPLETE
Eligibility E0: MERGED / OFFLINE ONLY
LR-v3.5.2: IMPLEMENTED / NATURAL ACCEPTANCE PENDING
```

No promotion, runtime, budget, L4/L5/L6 or 07:35 Collector-input change is authorized by this documentation checkpoint.
