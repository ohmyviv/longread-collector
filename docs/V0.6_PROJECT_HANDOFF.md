# Longread Collector v0.6 — Project Handoff

> Current engineering checkout for the longread Collector / daily recommendation project. Mutable canonical handoff: Google Doc `每日长文推荐 — Canonical Handoff`, id `1bhedf-8OWiWAWCYxwgboRfc5Et4yxDcZCi3de4sOisE`.

**Last substantive update: 2026-08-17.**

## 0. Authority / resume

For mutable facts verify in this order:

`merged code/contracts → open PR → GitHub Actions → scheduled natural evidence → live Sheet/config → docs → handoff → chat`.

If this file disagrees with higher-authority evidence, fix this file.

Repository: `ohmyviv/longread-collector`.

## 1. Current semantic and release state

Latest semantic code merge before docs-only synchronization:

```text
PR #94  4b581db494668a9da8897b88ced251e80dce6912
offline Standard Longread Eligibility E0
```

Relevant merges:

```text
PR #89  cec50fb56df3676a0b33927c9adc9f998a1a3141  Phase0B freshness reserve
PR #91  9ce9c43282f15b63963c56cd9f850b21e6436574  Phase0B audit persistence
PR #92  7baa4bc320296cfcc0b0f6ac25b1ba99d0bef8fd  Final Recall v1.2
PR #93  f408ccd801caff8cd51f228a4228b6fe0cd69c58  artifact-only full-funnel observability
PR #94  4b581db494668a9da8897b88ced251e80dce6912  offline Eligibility E0
PR #95  27efb2b6be21accf3158a694df5501203d65e387  docs baseline/promotion reconciliation
PR #96  dc111b37f5d9047c1ac20fd76eb91bc05a5ef208  docs Final Recall live-evidence reconciliation
```

Semantic identities:

```text
Collector runtime:          collector-v0.6-pr7.3.9
Canonical source:           canonical-source-v0.6-pr7.3.9
Canonical article resolver: canonical-article-resolver-v0.6-pr7.3.9
Publication:                canonical-publication-v0.6-pr7.3.7
Surface:                    canonical-surface-v0.6-pr7.3.4
Snapshot writer:            snapshot-persistence-v0.6-pr7.3.8
Phase0A audit:              snapshot-persistence-v0.6-phase0a
Frozen L5:                  editorial-judge-v0.6-pr7.2
Legacy control:             collector-v0.5.6m
Phase0B policy:             deadline-freshness-reserve-v0.6-phase0b.1
```

Production posture:

```text
mode: shadow
Transport Gate: READY
Editorial Gate: NOT_READY
Promotion Gate: SHADOW
article_cache production consumption: PROHIBITED
auto_promote_when_ready: FALSE
```

## 2. Frozen boundaries

Until an explicit reviewed change:

- no PR-8;
- L4 maintenance-only except P0/systemic P1;
- no production L5/L6 change to compensate for Discovery/reliability defects;
- native source cap stays 8;
- no Firecrawl/body/network budget expansion to manufacture Recall;
- 07:35 Editor does not consume `article_cache` before promotion;
- no auto-promotion;
- old raw candidate counts and legacy `53/75 not_discovered` are invalid Recall KPIs.

Additional freeze during the current second work package:

- do not modify LR-v3.5.2 before its first scheduled natural acceptance target on `2026-08-16 07:35 BJT`;
- do not manually trigger Collector;
- do not perform production/promotion wiring from offline evidence.

## 3. Phase0A — DONE / NATURAL ACCEPTED

```text
COL-20260813-184710-BJT-zh_evening
Actions 31692575791
expected=152
persisted=152
readback=true
full_snapshot_invariant=true
```

Promotion-grade strict snapshot integrity epoch starts `2026-08-13 18:47:10 BJT`. Earlier snapshots are diagnostic only when an item observation window crosses this epoch.

## 4. Phase0B — DONE / NATURAL ACCEPTED

Authoritative closure tuple, corrected from artifact + live ledger:

```text
Actions:          31837235761
collector_run_id: COL-20260815-041810-BJT-pre_report
started:          04:18:10 BJT
completed:        04:29:02 BJT
```

The older tuple `COL-20260815-042614-BJT-pre_report / 156` was stale and must not be reused.

Closure evidence:

```text
freshness_reserve:
  wired,newyorker,restofworld,quanta,atlantic,propublica
coverage_rotation:
  yale-e360,404media
sources total: 8
snapshot: 208/208
readback: true
full_snapshot_invariant: true
capture_gap: 0
shadow network requests: 0
shadow Firecrawl requests: 0
incremental shadow cost: 0
body fingerprint mismatches: 0
```

Exact policy marker, source reasons and scan ages are durable. A legacy fallback-ledger/schema-mapping mismatch remains separate debt and does not reopen Phase0B.

## 5. Collector Baseline Evaluation v1 — COMPLETE

Durable Google Doc id: `160hPZ0ni0pwYwlGia59MkMIBQJe4Dl2uotxLFwzY4G8`.

Primary baseline is the Phase0B closure run above.

Engineering decision:

```text
PASS AS SHADOW BASELINE
Transport/engineering: PASS
Product/editorial promotion: NOT READY
Promotion effect: NONE
```

PR #93 observation-aware funnel:

```text
12 attempted surfaces
→208 raw observations
→208 unique URL hints
→202 gate acquire +5 hard_reject +1 defer
→32 control-body observed
→32 Acquisition success
→32 Canonical success
→32 Editorial decision-eligible
→14 consider/actionable +18 low_value +0 recommend
→10 selected
Projection emitted: 0
```

Selection actions: `select_standard=10 / reject=23 / defer=175`. Most defers are body-not-observed boundaries, not Acquisition/editorial failures.

Reference 2026-08-14 pre_report was `190 raw → 32 control bodies → 30 acquisition success +2 partial → 14 actionable →10 selected`. The 2026-08-15 run supports mechanical Shadow stability, not Human Utility or promotion-grade Recall.

## 6. Final Recall v1.2 — FIRST LIVE SCHEDULED WRITE PASS / STRICT BASELINE ACCUMULATING

PR #92 replaced invalid global-window Recall measurement with item-level observation windows from actual publication date to the 07:35 cutoff, max 14 calendar days. Phase0A strict snapshot epoch determines whether an item is strict or partial observation.

Important new natural evidence:

```text
GitHub Actions: 31857563720
workflow: Final recommendation recall audit
execution_mode: write_to_sheets
report_date: 2026-08-15
status: SUCCESS
final_run_id: LR-20260815-0811-BJT-LRv35
```

The workflow ran `final_recall_audit_v12_runner`, wrote the v1.2 Sheets and verified one daily row plus all 8 detail rows. Therefore old wording that live v1.2 execution/write was still pending is superseded.

Headline 8-item result:

```text
eligible/registered/effective-route denominator=8
discovered=2
headline discovery recall=25%
editable=0
not_discovered=5
captured_but_rejected=2
manual_source_only=1
partial_observation_items=5
```

Promotion-grade strict subset:

```text
strict_effective_route_denominator=3
strict_discovered=1
strict_discovery_recall=33.33%
strict_editable=0
age_0_3d denominator=3
```

Strict items:

1. FT `AI frenzy drives Chinese tech valuations to multiples of US peers` — captured by RSS but prefilter rejected at `source_initial_cap_reserve`;
2. Reuters `While the world is distracted, China steps up its strategic game` — strict `not_discovered`;
3. Guardian extreme-weather/culture-war commentary — strict `not_discovered`.

The other five items cross the Phase0A strict epoch and remain `partial_observation`; they must not be pushed into the strict denominator.

Interpretation:

`1/3=33.33%` is an **early negative signal** for route/discovery quality, not a mature long-run Recall estimate. n=3 is too small for a route rewrite, new budget or promotion threshold. Continue prospective accumulation.

## 7. Collector Promotion Review v1 — NOT_READY / REMAIN SHADOW

Durable Google Doc id: `1C3H3aHSGLNI-7FYOsKtQsMw4htpx_wG8FuGuHiX_zus`.

Current matrix:

```text
A Engineering/Transport          PASS / READY
B Strict Recall                  STARTED / EARLY NEGATIVE SIGNAL / NOT PASS
C Human Utility                  PENDING
D Multi-day Editorial A/B        PENDING
E Eligibility readiness          PARTIAL / E0 ONLY
F Version/health reconciliation  FAIL / STALE LEGACY STATE
G Manual approval                NOT REQUESTED

Overall                          NOT_READY / remain SHADOW
```

Live `collector_health` still references legacy `editorial_gate_not_ready_v056j_review_pending` while runtime is `collector-v0.6-pr7.3.9`. `collector_evaluations` lacks a formal current-pr7.3.9 Human/Editorial release evaluation. Existing `collector_version_reconciliation_policy` makes this disagreement promotion-blocking.

Legacy thresholds (3 shadow days, 48h eligible, technical-domain count, fixed-fixture accuracy/precision/source-chase/wire-dedup) remain historical diagnostics, not sufficient v0.6 promotion criteria.

Recommended future path, not activated:

```text
Shadow
→ Production Candidate Input
   native Discovery remains parallel;
   origin/provenance retained;
   identical eligibility/L5/L6 rules
→ Primary Discovery
   only after production-context multi-day Recall/Human Utility/stability evidence
```

Every switch is manual; auto promotion remains off.

## 8. Full-funnel observability — MERGED

PR #93 is artifact-only. Missing acquisition event means body not observed by control, not Acquisition failure. `strong/editorial actionable` maps frozen L5 `recommend|consider`; `recommend` is separately high_editorial_value. Projection is not emitted and must not be fabricated.

## 9. Human Recommendation evidence — 80/80 COMPLETE

```text
强烈值得 9
值得 33
一般 18
不应推荐 20
Hit = 42/80 = 52.5%
```

Non-Hit attribution:

```text
upstream_eligibility=17
editorial_mismatch=12
preference_or_novelty=8
borderline_other=1
```

After upstream removal Hit=`42/63=66.7%`; editorial-evaluable conditional Hit=`42/55=76.4%`.

Wrong-medium/asset=6 are all human Rejects. `too_short=11` does not support a simple char hard gate. `too_newsy=9`, `too_shallow=3` support later narrow offline L5 depth/newsiness evaluation, but production L5 remains frozen while LR scheduled reliability is the current P0.

## 10. Standard Longread Eligibility — E0 MERGED / OFFLINE ONLY

PR #94 defines `eligible_standard / route_special / ineligible_standard / unknown` and does not wire production runtime. Length remains measurement-only.

Next isolated tracks:

- E1 high-confidence briefing/academic/video-first factual identity/routing;
- E2 substantive-length provenance + structural adequacy measurement;
- L5.1 later narrow depth/newsiness offline evaluation.

## 11. Scheduled LR path — separate P0

LR-v3.5.1 first scheduled natural run on 2026-08-15:

```text
LR-20260815-0735-BJT-LRv35
Coverage PASS
raw=8 / verified=2 / strong=2 / selected=2
model/reasoning requested=unspecified; observed=unavailable
final persistence FAIL: FINAL_PERSISTENCE_SHEETID_MISMATCH
```

Independent manual High:

```text
LR-20260815-0811-BJT-LRv35
requested GPT-5.6 Thinking + High
Coverage PASS
raw=26 / verified=23 / strong=13 / selected=8
persistence/readback/archive PASS
```

This rejects a two-item supply-scarcity explanation and confirms a scheduled execution-quality gap without proving High is the sole cause.

LR-v3.5.2 is implemented in the live Sheet runtime contract + 07:35 automation launcher for low-yield effort completion, delta-only checkpoints, dynamic tab locator, freeze/persistence/readback and truthful model audit. No Collector production wiring. Freeze LR-v3.5.2 until its first natural acceptance target `2026-08-16 07:35 BJT`.

## 12. Next evidence window

### P0 LR scheduled

Do not modify LR-v3.5.2 before the first natural 07:35 sample unless a new P0 requires it.

### Collector Shadow / Recall

Over the next 3–7 natural days:

1. remain Shadow;
2. generate daily artifact-only observation-aware funnels;
3. accumulate Final Recall v1.2 strict item-window evidence;
4. compare Collector universe with independent native/manual-High reference where available;
5. attribute repeated misses to registry/route/revisit/observation/L4/editorial/portfolio layers;
6. human-review only plausible Collector-exclusive high-value candidates;
7. derive Incremental Human-Useful Recall and report Chinese Recall separately.

Do not route-fix Reuters/Guardian from the first strict n=3 alone. Repeated timely-scan misses are required before route-quality changes.

### Promotion control plane

After sufficient prospective evidence, perform a separate reviewed v0.6 Promotion Reconciliation change that reclassifies stale v0.5.x health semantics, reconciles config/run/evaluation versions, writes a current release evaluation and defines Stage-2 rollback criteria. Only then consider manual `Production Candidate Input`; do not jump directly to Primary Discovery.

## 13. Current checkpoint

```text
Phase0A: DONE / NATURAL ACCEPTED
Phase0B: DONE / NATURAL ACCEPTED
Collector Shadow baseline: PASS / TRANSPORT HEALTHY
Final Recall v1.2: FIRST LIVE SCHEDULED WRITE PASS / STRICT BASELINE ACCUMULATING
Collector Promotion: NOT_READY / REMAIN SHADOW
Human evidence: 80/80 COMPLETE
Eligibility E0: MERGED / OFFLINE ONLY
LR-v3.5.2: IMPLEMENTED / NATURAL ACCEPTANCE PENDING
```

No runtime, budget, L4/L5/L6, promotion, `article_cache` or 07:35 Collector-input change is authorized by this docs checkpoint.

## 14. 2026-08-15 zh_midday natural evidence / second work package

New higher-authority scheduled evidence after the prior docs checkpoint:

```text
GitHub Actions: 31863911407
event: schedule
workflow slot: zh_midday / nominal 11:50 BJT
actual GitHub start: ~12:14 BJT
collector_run_id: COL-20260815-121459-BJT-zh_midday
checkout: dc111b37f5d9047c1ac20fd76eb91bc05a5ef208
workflow conclusion: SUCCESS
```

The approximately 25-minute schedule delay is reliability telemetry, not by itself a Collector pipeline failure.

Engineering/Transport initial evaluation:

```text
snapshot expected/persisted: 218/218
readback: true
full_snapshot_invariant: true
capture_gap: 0
shadow network requests: 0
shadow Firecrawl requests: 0
incremental shadow cost: 0
body fingerprint mismatch: 0
source cap: 8
freshness_reserve: yicai,jiemian-depth
coverage_rotation: deeptech,eeo,huxiu,chinanews-weekly,chinawriter,cyol-bingdian
```

Engineering/Transport therefore remains PASS. This sample does not reopen Phase0A/0B and does not change Promotion `NOT_READY / remain SHADOW`.

PR #93 observation-aware funnel preliminary read:

```text
218 raw observations
→217 gate acquire +1 hard reject
→32 control acquisition attempts
→31 shared control-body success +1 acquisition failure
→32 Canonical events
→32 Editorial evaluations
  recommend=2 / consider=15 / low_value=6 / reject=8 / insufficient_evidence=1
→select_standard=7 / source_chase=3 / reject=16 / defer=192
```

Of the 192 defers, 185 are `body_not_observed_in_control`; these are observation boundaries, not invented Acquisition/editorial failures.

Selected-set preliminary L4 review finds no listing/video/academic-paper/future-date/canonical-URL contamination; 7/7 selected items are written articles. Four source/relationship candidates require forensic classification before any engineering action:

1. MIT Technology Review China page: malformed `canonical_source="X）"`;
2. 国际在线: source identity includes `编辑：韩基韬`;
3. direct `news.cn` / 新华社 page is marked `secondary_republish` despite prior direct-Xinhua protection;
4. `chinawriter.com.cn` / 中国作家网 page is marked `secondary_republish` and requires same-host/source-evidence review.

The first two are at least metadata defects; the latter two remain forensic candidates until evidence profiles and trigger cues are inspected. Do not open PR-7.3.10 from this preliminary pass. L4 remains maintenance-only; only P0 or a systemic recommendation-affecting P1 pattern can justify production repair.

This midday run occurred after the 07:35 Final Recall cutoff. It cannot retroactively turn the same-day strict Reuters/Guardian misses into hits. The strict baseline remains denominator=3, discovered=1, recall=33.33%; any later capture is `late observation after cutoff` for attribution, not `captured in strict window`.

Second work package execution order:

```text
Task 0  zh_midday Natural Run Evaluation + L4 severity classification
Task 1  Final Recall strict 3-item miss forensic
Task 2  Promotion-grade Recall attribution contract
Task 3  Collector vs independent native/manual-High overlap evaluation framework
Task 4  Scheduled vs manual cohort audit
Task 5  Eligibility E1/E2 offline replay
Task 6  Promotion evidence schema / daily→cumulative aggregation
```

All tasks are analysis/offline work under the frozen boundaries above. They do not authorize LR-v3.5.2 edits, manual Collector runs, production L4/L5/L6 changes, budget expansion, article_cache production consumption, or promotion wiring.

## 15. Second work package — COMPLETE (Tasks 0–6)

The second work package is complete. Its outputs are diagnostic/measurement/offline evidence architecture only; they do not change the production posture.

### Task 0 — zh_midday Natural Run Evaluation / L4 severity

`COL-20260815-121459-BJT-zh_midday` remains Engineering/Transport PASS. Forensic review upgraded the four preliminary source/relationship candidates into one repeated systemic P1 family: `explicit-source / hosting-identity normalization gap`. Confirmed variants include malformed MIT Technology Review China source identity, CRI/editor metadata leakage, same-host ChinaWriter misclassification, and a direct `news.cn` Xinhua page violating the existing direct-Xinhua original-publication contract. No P0 was found. This family is now a legitimate maintenance candidate, but no production L4 repair or PR-7.3.10 is authorized from this work package.

### Task 1 — strict three-item miss forensic

The first strict `1/3` Final Recall cohort decomposes into different failure chains:

```text
FT valuation:
  prefilter / source_initial_cap_reserve
  evidence=confirmed

Reuters strategic article:
  source selected/attempted inside strict window
  complete immutable snapshot for that run
  zero Reuters raw observations emitted
  exact source_attempt vs surface_observation submechanism unresolved

Guardian culture-war/extreme-weather article:
  registered + effective route
  no successful evidenced source attempt inside item window
  source_selection / coverage boundary
```

Do not collapse these into a generic parser/Discovery failure.

### Task 2 — promotion-grade Recall attribution contract

PR #98 merged as `6abb909b773e7f33d151d324fe04c05da81d7f42`.

`docs/FINAL_RECALL_ATTRIBUTION_CONTRACT_V1.md` separates factual evidence from causal attribution and defines an earliest-evidenced-broken-stage taxonomy. `unknown/evidence_gap` is mandatory when evidence cannot distinguish mechanisms. Proposed future observability sidecars are `collector_source_attempts_v1` and `final_recall_attribution_v1`; neither is production-wired or implemented as a request-set change by Task 2.

### Task 3 — Collector vs independent native/manual-High overlap

PR #99 merged as `54ed1ac1146bda9c0ecaf3a4192982261c19ccd6`.

The independent 2026-08-15 manual-High reference is split into M0 raw=26, M1 verified=23, M2 Standard Longread=13 and M3 final=8. M2 is the candidate-universe coverage denominator; M3 remains Final Recall/product-output Recall.

```text
M2 diagnostic overlap: 2/13 = 15.38%
M2 promotion-grade strict subset: 1/6 = 16.67%
M3 strict Final Recall: 1/3 = 33.33%
```

The retrospective manual-High run is a strong diagnostic reference but not a perfectly prospective gold standard.

### Task 4 — scheduled vs manual cohort audit

PR #100 merged as `2f472e55ff5519a0a3041250966da2b332dab71d`.

Two same-day paired cohorts show a repeated execution-quality gap. Pooled 2026-08-13 + 2026-08-15 counts:

```text
scheduled raw/strong/selected: 13 / 5 / 5
manual raw/strong/selected:    40 / 26 / 16
manual/scheduled raw:          3.08x
manual/scheduled strong:       5.20x
manual/scheduled selected:     3.20x
```

Both manual reruns used less elapsed wall-clock time than their scheduled counterparts. Supply scarcity and coarse Coverage Gate sufficiency are rejected as primary explanations; scheduled execution quality/effort allocation is strongly implicated. A causal effect of High reasoning alone is not identifiable because scheduled model/reasoning is unspecified/unobserved and the treatment bundle is confounded.

### Task 5 — Eligibility E1/E2 offline replay

PR #101 merged as `2b785c31892ecb127cf3cb73754e0ec6e5ba3078`, CI PASS.

E1 is now an offline high-precision resolver: exact arXiv document identity routes special; narrow Guardian day-specific briefing identity is Standard-ineligible; video requires explicit upstream page/medium evidence and otherwise E1 abstains/falls back to E0. Retrospective durable metadata directly resolves 3/3 arXiv papers + 2/2 Guardian briefings; the historical New Beijing News video correctly remains unresolved without page-level video evidence rather than using the human label as machine input.

E2 is measurement-first. Historical `body_chars_read` is explicitly `legacy_approximate` and cannot support a hard gate. Trusted substantive measurement requires complete, untruncated, boilerplate-removed body evidence. The human-short family spans 2600–9000 old chars and overlaps known Hits at low lengths; the documented `<9500` replay captures 11/11 short labels but loses 22/42 Hits. E2 hard floor therefore remains NOT READY.

### Task 6 — promotion evidence schema / cumulative aggregation

PR #102 merged as `107d5b70c78bf3a1eb6bc64d3936b53b6185ff03`, CI PASS.

`promotion-evidence-v0.6-v1` defines explicit evaluation cohorts and daily evidence facts for Promotion Gates A–G. Cumulative rates are always recomputed from summed numerators/denominators; daily percentages are never averaged. Different semantic cohorts are never silently pooled. Partial observation, incomplete days, unresolved attribution and zero-denominator/not-measured states remain visible. The code deliberately contains no promotion decision, mode switch, config write, Sheets/network/workflow side effect or auto-promotion interface.

### Status after Tasks 0–6

```text
A Engineering/Transport          PASS / READY; prospective evidence accumulating
B Strict Recall                  STARTED / EARLY NEGATIVE SIGNAL / NOT PASS
C Human Utility                  PENDING
D Multi-day Editorial A/B        PENDING / frameworks ready
E Eligibility readiness          PARTIAL / E1 offline ready; E2 measurement-only
F Version/health reconciliation  FAIL / STALE LEGACY STATE
G Manual approval                NOT REQUESTED

Overall                          NOT_READY / remain SHADOW
```

Runtime boundaries remain unchanged: Collector `collector-v0.6-pr7.3.9` stays Shadow; frozen L5 remains `editorial-judge-v0.6-pr7.2`; no source-cap/budget expansion, production article_cache input, auto-promotion, PR-8 or production eligibility/L4/L5/L6 wiring occurred.

The next genuine action is evidence collection, not another immediate repair: keep LR-v3.5.2 frozen for its first scheduled natural acceptance on `2026-08-16 07:35 BJT`, continue prospective Collector Shadow/strict Recall evidence, and use the new attribution/overlap/pair/eligibility/promotion-evidence contracts for subsequent natural days. The systemic L4 P1 family remains recorded as a maintenance candidate; a production repair should be separately reviewed if repeated recommendation-affecting evidence justifies it.

## 16. 2026-08-16/17 scheduled evidence and controlled High rerun — CURRENT SUPERSEDING CHECKPOINT

This section supersedes Sections 11–13 and the closing sentence of Section 15 wherever their LR-v3.5.2 acceptance status or next-action wording conflicts with later evidence.

### 2026-08-16 scheduled finalization inconsistency

The 2026-08-16 scheduled LR-v3.5.2 run persisted startup state and partial downstream rows, but the terminal run state did not reconcile cleanly with candidate/final/history persistence. The best current RCA is probable execution-time exhaustion or other unhandled termination near finalization; a hard platform timeout was not proven.

Status:

```text
2026-08-16 natural sample: NOT PASS
classification: FINALIZATION INCONSISTENCY PENDING RCA
production repair authorized: NONE
```

A possible orphan-terminalizer/finalization repair was documented design-only. It was not authorized or wired into production.

### 2026-08-17 scheduled run — completion PASS

```text
run_id:                    LR-20260817-0735-BJT-LRv35
run_type:                  scheduled_auto
editor semantics:          LR-v3.5
runtime reliability:       LR-v3.5.2
raw / strong / selected:   25 / 2 / 2
lanes completed:           4
checkpoint1:               +16 / readback 16
checkpoint2:               +9 / cumulative 25
final freeze:              true
final/history/archive:     persisted
central readback:          pass
completion_validation:     pass
scheduled_runtime_acceptance: pass
prefreeze article_cache:   false
final_status:              success
```

This run proves LR-v3.5.2 can complete its intended runtime/persistence/readback contract under natural scheduled execution. Therefore the two-item result on 2026-08-17 is not explained by an incomplete run/finalization failure.

Scheduled final:

1. WIRED — `This Coin-Sized Device Can Hack a Boeing 737`
2. The Verge — `Rogue AI aren’t science fiction anymore`

### 2026-08-17 blind manual High rerun

```text
run_id:                    LR-20260817-0828-BJT-LRv35
run_type:                  manual_rerun
requested model:           GPT-5.6 Thinking
requested reasoning:       High
observed model/reasoning:  unavailable / unavailable
raw → date-valid → body/date verified → history-clean → strong → selected:
                           29 → 19 → 19 → 16 → 11 → 3
```

The rerun remained blind until final freeze: it did not read same-day scheduled candidate/final rows, article-level same-day scheduled chat evidence, or `article_cache`; Collector Shadow did not participate in Discovery, verification, scoring or selection.

Manual final:

1. The Guardian — `A bungled bomb plot in Poland exposed a spy – but whose spy was he?` — 94
2. WIRED — `This Coin-Sized Device Can Hack a Boeing 737` — 93
3. The Atlantic — `The 108-Degree Eviction` — 92

There were eight additional manual strong/final-caliber articles, so `manual final=3` must not be read as only three final-caliber international longreads existing that day.

### Chinese effective-supply / freshness bottleneck

Manual Chinese raw was `10`, but canonical-date-valid was `0`.

```text
manual explicit false-recency:   10/29 raw
scheduled comparable stale/false-recency examples: >=9/25 raw
```

Repeated patterns included current-looking publisher surfaces whose recovered standalone articles were actually from July 2026 or 2023. This supports a systemic `surface lead → canonical date` precision problem and a genuinely thin effective Chinese pool under the current freshness/product rules.

### Scheduled vs manual controlled comparison

```text
                         scheduled    manual
raw                         25          29
strong                       2          11
final                        2           3
raw identity overlap         4
exact URL overlap            2
strong overlap               1
final overlap                1
```

All 10 manual-only strong articles were absent from scheduled raw. The nearest evidence-backed category is therefore `discovery_miss`, not editorial scoring. The scheduled-only strong Verge article was absent from manual raw, showing bidirectional Discovery realization variance.

The one shared strong/final article, WIRED Boeing 737, was selected by both. There is no same-article evidence in this comparison supporting scheduled editorial conservatism as the primary cause.

Scheduled model/reasoning remained unspecified/unavailable, so a causal effect of High reasoning alone is not identifiable.

### Acquisition / dedup / Collector

Manual access telemetry was `DIRECT_OK=21 / PARTIAL_PREVIEW=3 / PAYWALL=5`. No manual item was proven final-caliber and excluded solely due Acquisition. Scheduled had several verification failures, including high-potential candidates, so Acquisition/canonical-body verification remains contributing rather than primary.

14-day exact-URL dedup removed 3 manual and 2 scheduled candidates; real contraction, but insufficient to explain the final-count collapse.

Post-freeze `article_cache` audit found exact overlap for the Guardian manual final only; WIRED and Atlantic did not exact-match. The audit occurred after freeze and did not alter `candidate_origin=native_search` or the payload.

### Root-cause decision

Primary explanation for **why the portfolio did not approach 10**:

> effective recent Chinese supply scarcity under current rules, amplified by publisher false-recency / canonical freshness precision contamination.

Secondary explanation for **why scheduled produced only 2 rather than the 3 international finals found manually**:

> scheduled Discovery recall / execution realization variance.

Contributing factors:

- Acquisition / canonical-body verification;
- 14-day history dedup.

Substantially ruled out for the 2026-08-17 sample:

- scheduled runtime incompleteness;
- editorial conservatism as primary cause;
- scarcity of high-quality international longreads.

### Test infrastructure follow-up

PR #105 merged as `7b970aa4513ad875c98b7c374d721668ae35e7ea` after PR #104 exposed a separate time-dependent CI defect: eight legacy synthetic test modules used hard-coded `2026-08-01/02` publication dates and silently depended on wall-clock time remaining inside the 14-day freshness window.

PR #105 added test-only deterministic clock control through the existing `begin_freshness_clock` / `end_freshness_clock` interface. Collector tests returned green. No freshness policy, runtime, config, L4/L5/L6, network/body budget, promotion or production behavior changed.

### Current checkpoint

```text
Phase0A:                         DONE / NATURAL ACCEPTED
Phase0B:                         DONE / NATURAL ACCEPTED
Collector Shadow baseline:       PASS / TRANSPORT HEALTHY
Collector Promotion:             NOT_READY / REMAIN SHADOW
Human evidence:                  80/80 COMPLETE
Eligibility E1/E2:               OFFLINE / MEASUREMENT ONLY
LR-v3.5.2 scheduled completion:  PASS on 2026-08-17
Discovery/freshness diagnosis:   OPEN / REQUIRES REPLICATION
Production change authorized:    NONE
```

Next minimum validation: on another naturally low-yield day, repeat the same blind controlled-rerun boundary and test whether (1) manual-only strong remains material (`>=5`, or scheduled recall of manual strong remains below roughly 50%), and (2) Chinese `surface lead → canonical recovered → date-valid → body verified` conversion remains very low, especially for 南方周末、界面、虎嗅、财新.

Do not change LR-v3.5.2, the 07:35 automation, Collector Shadow, L4, frozen L5, L6, source cap, network/Firecrawl/body budget, article_cache production consumption or promotion state from this single comparison.
